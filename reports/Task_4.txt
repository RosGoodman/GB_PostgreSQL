SELECT video.id
	first_name, last_name,
	video.url, video.size,
	photo.url
FROM users
	JOIN video ON video.owner_id = users.id
	LEFT JOIN photo ON photo.id = users.main_photo_id
ORDER BY size DESC LIMIT 10;